<h1>Business contacts you should know</h1>
<h2>Find Linkedin connections with the most mutual friends</h2>
<p>
	<% if user_signed_in? %>
	<ol><%= link_to "search", 'http://api.linkedin.com/v1/people-search' %></ol>
	<% else %>
  How it works:
  <ol>1. Login with Linkedin</ol>
  <ol>2. Approve app request</ol>
  <ol>3. View results</ol>
 	<ol><%= link_to 'Linkedin Signin',  user_omniauth_authorize_path(:linkedin) %></ol>
  <% end %>

  
</p>


<%= debug(session[:token]) if Rails.env.development? %>
<%= debug(session[:secret]) if Rails.env.development? %>
<%= debug(session[:pin]) if Rails.env.development? %>
<%= debug(session[:usertest]) if Rails.env.development? %>


